// Light and dark theme definitions for the UI framework
export { lightTheme } from './light';
export { darkTheme } from './dark';
import { lightTheme } from './light';
// Default theme (light)
export const theme = lightTheme;
