{
  "name": "@aboutrafi/lazy-panda-ui",
  "version": "0.1.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "description": "A simple UI framework for React Native (Expo compatible)",
  "keywords": [
    "react-native",
    "expo",
    "ui",
    "components"
  ],
  "author": "",
  "license": "MIT",
  "peerDependencies": {
    "expo": ">=44.0.0",
    "react": ">=17.0.0",
    "react-native": ">=0.64.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.9",
    "@types/react-native": "^0.72.8",
    "react": "^19.1.1",
    "react-native": "^0.80.2",
    "typescript": "^5.8.3"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc",
    "dist": "npm run build && cp -R src dist && find dist -type f -name '*.ts' -delete && find dist -type f -name '*.tsx' -delete"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  }
}
